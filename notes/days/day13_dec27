student laai add classrrom ko add student ma add garne


navbar ma click garesi agadi ko page ma jane banaune

templates--> myapp--> base.html
15 line ma
 href= {% url 'home_page' %}

 =============
 add studt click garda add stud form ma lanu ko lagi
 step1:
 urls.py(forms)
 from .views import add_student
 path("add-student/", add_student, name="add_student")

 step 2:
 forms--> templates--> forms--> add_student.html

 a) extends garne
{% extends 'myapp/base.html' %}
{% block content %}
    b) form banaune  bootstrap baat
        format milauna ctrl alt +l
{% endblock %}
b) form banaune
===============add student click garda add_classroom.html ma lana parchh
 <h1>classroom ko tala
 <a href="{% url 'add_student' %}"
 ======

 <h1>Add Student</h1><br>
 text
 placeholder
 name

 email-----
 placeholder= email
 name= email    sabai milaune

 ==========
 submit garepacchi student add huna parchh

  <h1>Add Student</h1><br>
    <form method="post" action="{% url 'add_student' %}"
 href ma chnge garne

 ====
 form submit garesi data ma jana
a)views.py ma post method baat data tanne
b) db ma janchh
    student = Student.objects.create(name=name, age=age, email=email,
            address=address, classroom_id=classroom_id)
c) student page ko url name rakhne
    return redirect("student")
    myapp -->urls.py--> urlspatterns bhitra--> name="student"),
----------- add student garaer data add garne---------check-----
=============
pricing click garda pricing ko page ma jane
task
===========
aba student laai classroom ma assign garne
classroom banar dropdown aaos(multiple choice name)
----------
d)
div class="form-group col-md-4">
                <label for="inputState">Classroom</label>
e) views.py  ma data send garne
classrooms = ClassRoom.objects.all()
return render(request, template_name="forms/add_student.html", context={"classrooms": classrooms})

f) add_student.html
 <div class="form-group col-md-4">
                <label for="inputState">Classroom</label>
                <select id="inputState" class="form-control">
                    {% for classroom in classrooms %}
                        <option selected >"{{ classroom.name }}"</option>
                    {% endfor %}
                </select>
            </div>

================
student add garda classroom chhose garne bela data featch garne
g) <select id="inputState" class="form-control" name="classroom_id">
                    {% for classroom in classrooms %}
                        <option selected value="{{ classroom.id }}">{{ classroom.name }}</option>
                    {% endfor %}
                </select>
note:
aba BE ma four select garda four jadaina tesko id janchh
 h)
 aba views.py ma
 classroom_id = request.POST.get("classroom_id")
 student = Student.objects.create(name=name, age=age, email=email, address=address, classroom_id=classroom_id)
============
classrom eita colom rakhna